const total = 100;
const ordered = 50;
if (total < ordered) {
  const message = `На складе недостаточно товаров!`;
  console.log(message);
} else {
  const message = `Заказ оформлен, с вами свяжется менеджер`;
  console.log(message);
}
